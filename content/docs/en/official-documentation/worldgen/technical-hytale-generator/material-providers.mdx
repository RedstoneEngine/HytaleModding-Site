---
title: Material Providers
description: Determines which Material to use where.
authors:
  - name: "Hypixel Studios Canada Inc."
    url: "https://hytale.com"
---

# Material Providers

### Constant

Provides a constant Material.

Parameters:

<ParameterList
  parameters={[
    {
      name: "Material",
      type: "Material slot",
      description: "",
    },
  ]}
/>

---

### Transparent

The lack of any Material.

Parameters: None.

---

### Solidity

Separates terrain into Solid and Empty blocks. Provides a Material Provider slot for each selection.

Parameters:

<ParameterList
  parameters={[
    {
      name: "Solid",
      type: "Material Provider slot",
      description: "Where the terrain's Density value is greater than 0.",
    },
    {
      name: "Empty",
      type: "Material Provider slot",
      description:
        "Where the terrain's Density value is smaller or equal to 0.",
    },
  ]}
/>

---

### Queue

Goes through a queue of Material Provider slots from highest to lowest priority. If a Material Provider in the queue doesn’t provide a block type, then the next one in the queue is queried. If no slot provides a block, then the Queue Material Provider will not provide a block. The query stops at the first slot that provides a block.

Parameters:

<ParameterList
  parameters={[
    {
      name: "Queue",
      type: "list of Material Provider slots",
      description: "Slots are queried from top to bottom.",
    },
  ]}
/>

---

### SimpleHorizontal

Applies the child Material Provider to a vertical range.

If a BaseHeight is provided, the TopY and BottomY values are relative to the referenced DecimalConstant. Otherwise, they’re relative to the world’s Y coordinate.

Parameters:

<ParameterList
  parameters={[
    {
      name: "TopY",
      type: "integer",
      description:
        "Y coordinate. Defines the vertical top <strong>exclusive</strong> limit of the region.",
    },
    {
      name: "Top BaseHeight",
      type: "string",
      description: "DecimalConstant name.",
    },
    {
      name: "BottomY",
      type: "integer",
      description:
        "Y coordinate. Defines the vertical bottom <strong>inclusive</strong> limit of the region.",
    },
    {
      name: "Bottom BaseHeight",
      type: "string",
      description: "DecimalConstant name.",
    },
    {
      name: "Material",
      type: "Material Provider slot",
      description: "Slot to query for the selected region.",
    },
  ]}
/>

---

### Striped

Applies a Material Provider slot to a set of horizontal stripes of blocks of varying thickness and position. Below is an example of basalt stripes in a stone cliff.

![](/assets/official-documentation/worldgen/technical-hytale-generator/material-providers/basalt-stripes-example.png)

Parameters:

<ParameterList
  parameters={[
    {
      name: "Stripes",
      type: "list of stripe assets",
      description:
        "Each stripe contains <code>TopY</code> and <code>BottomY</code>.",
    },
    {
      name: "Stripes[].TopY",
      type: "integer",
      description: "World Y value. Inclusive.",
    },
    {
      name: "Stripes[].BottomY",
      type: "integer",
      description: "World Y value. Inclusive.",
    },
    {
      name: "Material",
      type: "Material Provider slot",
      description: "Slot to query for the selected region.",
    },
  ]}
/>

---

### Weighted

Picks the Material Provider slot to query from a list. Each slot has a weight that determines the likelihood of being picked.

Parameters:

<ParameterList
  parameters={[
    { name: "Seed", type: "string", description: "" },
    {
      name: "SkipChance",
      type: "float (0.0–1.0)",
      description:
        "Percentage of blocks to skip. When skipped, this provider returns no block and does not pick a slot.",
    },
    {
      name: "WeightedMaterials",
      type: "list of weighted Material Provider slots",
      description: "",
    },
    {
      name: "WeightedMaterials[].Weight",
      type: "number (> 0)",
      description: "Higher value increases likelihood of being picked.",
    },
    {
      name: "WeightedMaterials[].Material",
      type: "Material Provider slot",
      description: "",
    },
  ]}
/>

---

### FieldFunction

Selects a 3D region using a noise function and value delimiters. The delimiters link a Material Provider slot to a specific range of values in the noise function.

Parameters:

<ParameterList
  parameters={[
    {
      name: "FieldFunction",
      type: "field function asset",
      description:
        "Defines the field value for each block. Same asset type used by DAOTerrain density.",
    },
    {
      name: "Delimiters",
      type: "list of delimiters",
      description: "Higher entries have higher priority.",
    },
    {
      name: "Delimiters[].From",
      type: "float",
      description: "Minimum limit of the delimiter’s range.",
    },
    {
      name: "Delimiters[].To",
      type: "float",
      description: "Maximum limit of the delimiter’s range.",
    },
    {
      name: "Delimiters[].Material",
      type: "Material Provider slot",
      description: "Slot linked to this delimiter.",
    },
  ]}
/>

---

### SpaceAndDepth

Allows placing layers of blocks on a terrain’s floor or ceiling surfaces. Layers of Material are piled on top of each other inside the surface (floor or ceiling), like a cake. There are multiple Layer types, each providing a unique way to define thickness. Below is an example with two layers in the floor of our terrain.

![](/assets/official-documentation/worldgen/technical-hytale-generator/material-providers/depth-and-space-example.png)

Conditions let you skip this Material Provider when they’re not met. An example could be a Condition that only places grass if there are at least 5 empty blocks above the surface.

Parameters:

<ParameterList
  parameters={[
    {
      name: "LayerContext",
      type: "string",
      description:
        "Context the layers are applied on. Values: <code>DEPTH_INTO_FLOOR</code>, <code>DEPTH_INTO_CEILING</code>.",
    },
    {
      name: "MaxExpectedDepth",
      type: "positive integer",
      description:
        "Optimization hint. Set to max expected combined layer depth. Too small can cut off layers. Too large can hurt performance.",
    },
    {
      name: "Condition",
      type: "Condition slot",
      description: "Provider returns no block if the Condition fails.",
    },
    {
      name: "Layers",
      type: "list of Layer objects",
      description: "Stacked top-to-bottom as depth increases into the context.",
    },
  ]}
/>

---

### Condition

A Condition object checks whether the environment is valid to determine whether the Material Provider should run for the current block. If the Condition fails for a block, the Material Provider returns no block.

An example Condition could check if there are at least 5 blocks of empty space above the floor. This is useful if you want dirt in tight spaces instead of grass. An example is shown below.

![](/assets/official-documentation/worldgen/technical-hytale-generator/material-providers/condition-example.png)

{/* MIRO BOARD LINK */}

The Condition’s `ContextToCheck` parameter selects which context value the Condition queries. In the example above, we’re checking the `SPACE_ABOVE_FLOOR` context, which is the number of blocks above the floor. There are currently two possible context values you can use:

- `SPACE_ABOVE_FLOOR`
- `SPACE_BELOW_CEILING`

There are multiple Condition types that allow you to compose more complex Conditions from simpler ones. Below are the different types.

**EqualsCondition**

Validates only if a context value is equal to the Condition’s configured value.

Parameters:

<ParameterList
  parameters={[
    {
      name: "ContextToCheck",
      type: "string (<code>SPACE_ABOVE_FLOOR</code> | <code>SPACE_BELOW_CEILING</code>)",
      description: "Which context value the Condition queries.",
    },
    {
      name: "Value",
      type: "integer",
      description: "Condition passes only if the context value equals this.",
    },
  ]}
/>

**GreaterThanCondition**

Validates only if a context value is greater than the Condition’s configured threshold.

Parameters:

<ParameterList
  parameters={[
    {
      name: "ContextToCheck",
      type: "string (<code>SPACE_ABOVE_FLOOR</code> | <code>SPACE_BELOW_CEILING</code>)",
      description: "Which context value the Condition queries.",
    },
    {
      name: "Threshold",
      type: "integer",
      description:
        "Condition passes only if the context value is greater than this.",
    },
  ]}
/>

**SmallerThanCondition**

Validates only if a context value is smaller than the Condition’s configured threshold.

Parameters:

<ParameterList
  parameters={[
    {
      name: "ContextToCheck",
      type: "string (<code>SPACE_ABOVE_FLOOR</code> | <code>SPACE_BELOW_CEILING</code>)",
      description: "Which context value the Condition queries.",
    },
    {
      name: "Threshold",
      type: "integer",
      description:
        "Condition passes only if the context value is smaller than this.",
    },
  ]}
/>

**AndCondition**

Logical AND Condition. Validates only if all child Conditions validate.

Parameters:

<ParameterList
  parameters={[
    {
      name: "Conditions",
      type: "list of Condition slots",
      description: "Condition passes only if all child Conditions pass.",
    },
  ]}
/>

**OrCondition**

Logical OR Condition. Validates if any child Condition validates.

Parameters:

<ParameterList
  parameters={[
    {
      name: "Conditions",
      type: "list of Condition slots",
      description: "Condition passes if any child Condition passes.",
    },
  ]}
/>

**NotCondition**

Logical NOT Condition. Validates only if its child Condition does not validate.

Parameters:

<ParameterList
  parameters={[
    {
      name: "Condition",
      type: "Condition slot",
      description:
        "Single slot (not a list). Condition passes if the child Condition fails.",
    },
  ]}
/>

**AlwaysTrueCondition**

This Condition type always validates.

---

### Layer

In the SpaceAndDepth Material Provider type, Layers are stacked into the depth of the floor or ceiling. Layers have a thickness and a Material Provider.

Below are some examples that illustrate how layers work.

Example 1:

- Layer 1: grass of thickness 1.

![](/assets/official-documentation/worldgen/technical-hytale-generator/material-providers/grass-layer-example.png)

Example 2:

- Layer 1: grass of thickness 1.
- Layer 2: dirt of thickness ranging from 2 to 3.

![](/assets/official-documentation/worldgen/technical-hytale-generator/material-providers/dirt-layer-example.png)

Example 3:

- Layer 1: mud of thickness 1 to 5 defined by a 2D simplex noise function.
- Layer 2: dirt of thickness ranging from 2 to 3.

![](/assets/official-documentation/worldgen/technical-hytale-generator/material-providers/mud-layer-example.png)

Example 4: This example shows how this can work not only on floors but also on ceilings by changing the LayerContext value to `DEPTH_INTO_CEILING`.

- Layer 1: dirt of thickness in range 2 to 3.

![](/assets/official-documentation/worldgen/technical-hytale-generator/material-providers/ceiling-layer-example.png)

End of examples.

Below are the supported Layer types.

**ConstantThickness**

Has the same thickness everywhere.

Parameters:

<ParameterList
  parameters={[
    { name: "Material", type: "Material Provider slot", description: "" },
    {
      name: "Thickness",
      type: "integer (>= 0)",
      description: "Thickness in blocks.",
    },
  ]}
/>

**RangeThickness**

The thickness varies per XZ coordinate and is defined by a range of possible values.

Parameters:

<ParameterList
  parameters={[
    { name: "Material", type: "Material Provider slot", description: "" },
    {
      name: "RangeMin",
      type: "integer (>= 0)",
      description: "Minimum thickness in blocks.",
    },
    {
      name: "RangeMax",
      type: "integer (>= 0)",
      description: "Maximum thickness in blocks (inclusive).",
    },
    { name: "Seed", type: "string", description: "" },
  ]}
/>

**WeightedThickness**

The thickness varies per XZ coordinate and is picked from a list of weighted thickness values.

Parameters:

<ParameterList
  parameters={[
    { name: "Material", type: "Material Provider slot", description: "" },
    {
      name: "PossibleThicknesses",
      type: "list of weighted thickness values",
      description: "Used to pick a thickness per XZ.",
    },
    {
      name: "Seed",
      type: "string",
      description: "Seed used when picking the thickness.",
    },
  ]}
/>

**NoiseThickness**

The thickness varies per XZ coordinate and is determined by a noise function tree. The thickness value comes from the function output. In the example below, the output is normalized from 0 to 5. This makes the thickest points about 5 blocks.

Parameters:

<ParameterList
  parameters={[
    { name: "Material", type: "Material Provider slot", description: "" },
    {
      name: "ThicknessFunctionXZ",
      type: "noise function tree",
      description:
        "Sampled as a 2D function on the XZ plane (Y is always <code>0</code>).",
    },
  ]}
/>

---

### Imported

Imports an exported Material Provider.

Parameters:

<ParameterList
  parameters={[
    {
      name: "Name",
      type: "string",
      description: "The exported Material Provider.",
    },
  ]}
/>
